# RaverMVP Public Feed

Public static feed host for RaverMVP weekly event data.

- App feed: `feed/v1/feed.draft.json`
- Ingest report: `report/ingest_report.json`

The private app repository remains separate. This repository only hosts JSON artifacts that the app can fetch at runtime.
